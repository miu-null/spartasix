<form class="clubs-list">
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th>글번호</th>
        <th>제목</th>
        <th>글쓴이</th>
        <th>모집인원</th>
        <th>등록일</th>
        <th>좋아요</th>
      </tr>
    </thead>

    <tbody>
      <% slicedData.forEach(data=> { %>
        <tr class="clubs">
          <td class="clubs-detail">
            <%= data.clubId %>
          </td>
          <td class="clubs-detail">
            <%= data.title %>
          </td>
          <td class="clubs-detail">
            <%= data.userId %>
          </td>
          <td class="clubs-detail">
            <%= data.maxMembers %>
          </td>
          <td class="clubs-detail">
            <%= data.createdAt %>
          </td>
          <% }) %>
        </tr>
    </tbody>
  </table>
</form>


<div id="pagination">
  <nav aria-label="Page navigation">
    <ul class="pagination justify-content-center">
      <% if (unitStart - 1> 0 ) { %>
        <li class="page-item">
          <a class="page-link" href="clubs?page=<%= unitStart -1 %>" tabindex="-1"
            aria-disabled="true">이전</span>
          </a>
        </li>
        <% } %>
          <% for (let i=unitStart; i <=unitEnd; i++) { %>
            <% if (i <=lastPage) { %>
              <% if ( i===page) { %>
                <li class="page-item active" aria-current="page">
                  <span class="page-link">
                    <%= i %>
                  </span>
                </li>
                <% } else { %>
                  <li class="page-item"><a class="page-link" href="clubs?page=<%= i %>">
                      <%= i %>
                    </a></li>
                  <% } %>
                    <% } %>
                      <% } %>
                        <% if (unitEnd <=lastPage) { %>
                          <li class="page-item">
                            <a class="page-link" href="clubs?page=<%= unitEnd +1 %>">다음
                            </a>
                          </li>
                          <% } %>
    </ul>
  </nav>
</div>



<section id="search" class="search-wrap">
  <form action="/search/users" class="search-form">
    <!-- <div class="col-10 col-lg-auto mb-3 mb-lg-0 me-lg-3"> -->
    <input type="search" id="term" name="term" class="form-control" placeholder="🔍(테스트)유저 검색 : email, 닉네임"
      aria-label="Search" pattern=".{1}|.{1,10}" required title="최소 1자 이상 입력해야 합니다">
  </form>
</section>

</div>